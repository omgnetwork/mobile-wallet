import { AppNavigator, _WarpPortalNavigator } from './navigator'
import { createAppContainer } from 'react-navigation'

const AppContainer = createAppContainer(AppNavigator)

export default AppContainer
